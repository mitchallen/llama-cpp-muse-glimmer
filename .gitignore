# llama-server output, written by `make run` via tee; LOG defaults to server.log
# but is overridable, so ignore any log
*.log

# generated per run by `make opencode-sandbox`; hardcodes absolute paths
opencode.sandbox.json
